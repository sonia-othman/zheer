<?php

return [

    'paths' => ['api/*', 'devices', 'sanctum/csrf-cookie'], // Add your routes here

    'allowed_methods' => ['*'], // Allow all HTTP methods

    'allowed_origins' => ['*'], // Allow all origins (you can restrict this to specific domains)

    'allowed_origins_patterns' => [],

    'allowed_headers' => ['*'], // Allow all headers

    'exposed_headers' => [],

    'max_age' => 0,

    'supports_credentials' => false, // Set to true if you need cookies or authentication
];